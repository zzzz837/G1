# EDCR-Net 项目总路线与进展总览

**项目名称**：基于显式复合退化建模与条件化频带残差修复的语音增强方法  
**英文建议名**：EDCR-Net (Explicit Degradation Conditioning and Residual Refinement Network)  
**当前工作目录**：`D:\workshop\G1`  
**当前时间点定位**：已完成方法框架搭建与小规模真实语音流程验证，正在进行全量特征缓存。  

---

# 1. 项目目标（一句话说明）

本项目的目标不是重新训练一个更大的 GTCRN，而是在**冻结官方 GTCRN 主干**的基础上，增加一个**显式复合退化诊断 + 条件化频带残差修复**链路，使模型能够针对通信场景中的多种复合退化（噪声、窄带、量化等）进行更有针对性的增强。

核心逻辑：

```text
Composite degraded speech
        ↓
     Frozen GTCRN
   ├── enhanced_base
   └── bottleneck stats
            ↓
 Degradation Estimator
   ├── noise_present
   ├── snr_pred
   ├── bandwidth_class
   └── bit-depth_class
            ↓
 condition embedding
            ↓
 Band-aware residual refinement
            ↓
 enhanced_final = enhanced_base + conditioned residual
```

---

# 2. 当前论文定位（已确认）

## 正式定位

> **基于显式复合退化建模与条件化频带残差修复的语音增强方法**

## 已确认的研究方向选择

- **1-C**：基于显式退化建模的条件化频带残差增强
- **2-C**：退化估计器既独立评价，又服务于残差修复
- **3-A**：正式模型坚持冻结 GTCRN 主干
- **4-C**：采用轻量 Multi-expert 频带残差（但必须保持轻量）
- **5-D**：综合平衡，但优先级是  
  **客观增强性能 > 机制可解释性 > 轻量部署性**
- **6-A**：先完成正式缓存和退化估计器训练，再决定后续最优特征来源与残差结构

## 研究主线（明确）

**核心创新不是“再加一个 residual block”**，而是：

> **让显式退化诊断结果真正驱动条件化频带残差修复。**

---

# 3. 原始 GTCRN 与我们的扩展关系

## 原始 GTCRN 是什么

官方 GTCRN 是一个超轻量语音增强主干：

- 参数量约 **48.2K**
- 计算量约 **33 MMAC/s**
- 输入：16kHz 语音 STFT
- 输出：增强频谱
- 主要应用：通用语音增强

官方文件：
- `gtcrn.py`
- `infer.py`
- `loss.py`

这些文件在项目中**保持不修改**，作为严谨可比的官方基线。

## 我们做了什么扩展

我们没有改 GTCRN 主干，而是在其**后面**加了两层扩展：

### 扩展 1：退化估计器
根据 GTCRN 的瓶颈统计特征预测：
- 有无噪声
- SNR
- 带宽类别
- 量化位深类别

### 扩展 2：条件化残差补偿模块
根据退化诊断结果，决定如何对 GTCRN 基础增强结果做轻量修复。

---

# 4. 总体技术架构

## 当前实现的完整逻辑

```text
干净语音
  ↓
复合退化生成器（Phase 2）
  ├── 加性噪声
  ├── 带宽限制
  ├── 低通
  ├── 重采样往返
  └── 量化失真
  ↓
STFT
  ↓
Frozen GTCRN（Phase 1 + Phase 3）
  ├── enhanced_base
  └── dpgrnn2 bottleneck stats (32维)
        ↓
Degradation Estimator（Phase 3）
  ├── noise_present
  ├── snr_pred
  ├── bandwidth_class
  └── bit_class
        ↓
condition embedding
        ↓
Adaptive Residual Module（Phase 4）
        ↓
enhanced_final
        ↓
iSTFT
        ↓
最终增强语音
```

---

# 5. 已完成阶段总览

## Phase 1：官方 GTCRN CPU 基线复现
### 已完成
- 官方模型加载与推理成功
- CPU-only 运行
- `gtcrn.py / infer.py / loss.py` 保持不变
- bit-exact 基线验证通过
- baseline report 已记录

### 结论
- 官方基线可靠，可作为论文后续所有对比的统一参照系
- 当前基线最大误差：**0.0**

---

## Phase 2：复合退化生成器与标签系统
### 已完成
实现了以下退化：
- 加性噪声
- 低通滤波
- 重采样往返
- 带宽限制
- 量化失真

预设：
- clean
- light
- medium
- heavy

标签：
- `noise_present`
- `snr_target`
- `bandwidth_class`
- `bit_class`
- `degradation_mask`

### 测试
- `work2/tests/test_degradation.py`
- **39/39 测试通过**

### 结论
当前复合退化生成器已足以支撑论文中“复合退化建模”这一部分。

---

## Phase 3：冻结 GTCRN + 显式退化估计器
### 已完成
- `FrozenGTCRNFeatureExtractor`
- `DegradationEstimator`
- 多任务损失
- feature cache 脚本
- dataset
- 评估脚本
- 冒烟测试
- VCTK 小规模真实语音验证

### 模型参数
- 退化估计器参数量：**1,803 ~ 1,985**（根据版本）

### 能预测的任务
- 噪声有无（二分类）
- SNR（回归）
- 带宽（三分类）
- 位深（四分类）

### 当前小规模 VCTK 真实语音结果（小样本验证）
基于 5 个 VCTK 样本、15 训练样本的快速试验结果：
- noise accuracy：接近 **100%**
- bandwidth accuracy：约 **60%~100%**
- bit accuracy：约 **20%~100%**（样本太少，不稳定）
- SNR MAE：约 **4.45 dB**（小样本快速训练结果）

### 结论
GTCRN 的 `dpgrnn2` 瓶颈统计特征中明显包含退化信息，退化估计器方向成立。

---

## Phase 4：条件化自适应残差补偿框架
### 已完成
- `AdaptiveResidualModule`
- `GTCRNAdaptive`
- `AdaptiveResidualLoss`
- `run_adaptive_smoke.py`
- 自适应残差的单元测试

### 参数量
- 当前 residual 模块：约 **2,184** 参数
- 当前 estimator + residual 总额外参数：约 **4K**

### 当前状态结论
- 管线已经打通
- 代码可运行
- 冒烟训练可跑通
- 真正的论文级效果还需要全量真实数据训练和正式评测

### 当前风险
单 residual 分支的创新力度有限，因此后续建议升级为**轻量多频带专家残差模块**。

---

# 6. 当前数据进展（真实数据）

## VCTK 数据集
### 已完成
- 下载完整 VCTK 数据集：`10.4GB`
- 解压完成：`88,537` 文件
- 重采样完成：`44,247` 个 16kHz wav
- 说话人数：`109`
- 总大小：约 `4.73GB`

### 当前目录
- 原始压缩包：`data/vctk/OpenDataLab___VCTK/raw/VCTK-Corpus.zip`
- 解压目录：`data/vctk_raw/VCTK-Corpus/`
- 重采样目录：`data/vctk_16k/`

## 当前训练前缓存进度（必须重点关注）
### 正在进行
`cache_degradation_features.py`

### 当前进度（最近一次检查）
- 已缓存条目：**148,804**
- 已覆盖源语音：**37,201 / 44,247**
- 当前进度：**84.1%**

### 结论
缓存已经接近完成，离正式 estimator 训练只差最后约 15.9%。

---

# 7. 为什么要先缓存？

缓存不是训练本身，而是**训练前的特征预处理阶段**。

## 目的
把每条复合退化语音都先通过 Frozen GTCRN 一次，提取出：
- stats(32维)
- 标签
- split 信息

然后后面训练 estimator 时，不需要重复跑 GTCRN。

## 好处
- 节省训练时间
- 稳定实验设置
- 方便做多个 estimator / loss / 消融实验
- 便于断点续跑

## 当前判断
对于你的项目，缓存是必要步骤，不能跳过。

---

# 8. 当前剩余任务路线（正式版）

## 阶段 A：把缓存跑完（当前正在进行）
### 目标
获得完整的 `outputs/estimator_cache/`

### 完成标志
- `index.jsonl` 生成完毕
- `cache_summary.json` 输出
- train / valid / test 数量正确

---

## 阶段 B：正式训练退化估计器
### 目标
证明 `dpgrnn2` 统计特征对退化因素具有可探测性。

### 需要产物
- `best_model.pt`
- `last_model.pt`
- `history.csv`
- `metrics.json`

### 必须报告的指标
- noise accuracy / F1
- bandwidth accuracy / confusion matrix
- bit-depth accuracy / confusion matrix
- SNR MAE / RMSE / Pearson r

### 这一步的论文结论
> GTCRN 冻结主干的内部统计特征已经足以表达显著的复合退化状态。

---

## 阶段 C：做特征来源对比实验
### 必做原因
要证明：为什么是 `dpgrnn2`，而不是别的层。

### 比较对象
- 输入频谱统计
- encoder 中层统计
- dpgrnn1
- dpgrnn2
- enhanced_base 统计
- 多层联合统计

### 决策逻辑
- 如果 dpgrnn2 最优：维持当前方法
- 如果多层更优：后续升级为多层联合退化探测

---

## 阶段 D：升级 residual 为轻量多频带专家
### 当前单 residual 不足
当前 residual 只有一个分支，创新力度偏弱。

### 目标结构
建议使用 **3 个频带专家**：
- low-band expert
- mid-band expert
- high-band expert

融合形式：

\[
R = \alpha_L R_L + \alpha_M R_M + \alpha_H R_H
\]

其中：
\[
[\alpha_L, \alpha_M, \alpha_H] = softmax(G(e_d))
\]

### 目标约束
- residual 模块总参数控制在 **4K~5K**
- estimator + residual 总参数控制在 **< 7K**

---

## 阶段 E：正式训练 adaptive residual
### 推荐正式训练策略
第一版正式实验：
- GTCRN 冻结
- estimator 冻结
- 只训练 residual experts

### 原因
这样最容易证明：
> 显式退化条件能否真正驱动有意义的频带修复。

### 第二版补充实验
- estimator + residual 联合训练

---

## 阶段 F：核心实验对比
### 对比组
必须做：
1. Frozen GTCRN
2. GTCRN + 普通 residual（无条件）
3. GTCRN + EDCR-Net（有条件）

可选增强：
4. Oracle labels 驱动 EDCR-Net
5. Predicted labels 驱动 EDCR-Net

### 指标
- PESQ
- STOI
- SI-SDR / SI-SNR
- LSD / HF-LSD（可选）
- 参数量 / RTF / CPU 时间

---

## 阶段 G：分组分析
### 按 severity 分组
- clean
- light
- medium
- heavy

### 按标签再分组
- noisy / no-noise
- full band / 6k / 4k
- 16 / 12 / 10 / 8 bit

### 目标
证明提升是否集中在：
- 中重度复合退化
- 带宽受限语音
- 重量化语音

---

## 阶段 H：消融实验
### 必做
1. 去掉 noise head
2. 去掉 SNR head
3. 去掉 bandwidth head
4. 去掉 bit-depth head
5. 去掉高频损失
6. 去掉残差正则
7. 全局 α vs 分频带 α
8. 单 residual vs 多专家 residual

---

# 9. 当前最重要的实验性结论（阶段性）

## 已经可以下结论的部分

### 结论 1
复合退化建模这个方向成立，当前系统已经具备：
- 退化合成
- 退化标签化
- 可复现随机控制
- 全流程工程闭环

### 结论 2
GTCRN 的冻结瓶颈统计特征中明显包含退化信息。
这一点已经通过小规模 VCTK 验证初步成立。

### 结论 3
条件化 residual 管线已经打通，但其论文级性能效果尚未通过正式大规模训练和对照实验验证。

### 结论 4
真实数据端到端流程（下载→解压→重采样→manifest→cache→训练）已经全部具备工程可执行性。

---

# 10. 目前有哪些内容还不能作为最终论文结论

以下内容目前**不能**直接写成最终实验结论：

- adaptive residual 当前对 VCTK 5样本验证时优于 base GTCRN
- estimator 的小样本 100% 噪声精度可以代表最终结果
- 合成噪声冒烟结果可以代表真实通信场景

这些只能算：
- 管线验证结果
- 工程可行性结果
- 早期探索性结果

正式论文结论必须来自：
- 全量缓存
- 正式训练
- 正式评测
- 完整对比与消融

---

# 11. 训练时间预估（CPU）

## 在你当前电脑上（i5-12450HX，CPU-only）

| 阶段 | 预计时间 |
|------|----------|
| 完整缓存 | ~6-12 小时（与你当前实际速度接近） |
| 退化估计器训练 | ~1 小时 |
| residual 训练 | ~3-5 小时 |
| 完整一轮实验（不含重复调参） | ~1 天 |

### 当前状态
缓存已跑到 84.1%，说明实际速度比最保守估计更快。

---

# 12. 中断与恢复方式（必须记住）

## 环境设置（每次新终端）
在 PowerShell 中：

```powershell
cd D:\workshop\G1
$env:KMP_DUPLICATE_LIB_OK = "TRUE"
```

在 CMD 中：

```cmd
cd /d D:\workshop\G1
set KMP_DUPLICATE_LIB_OK=TRUE
```

---

## 缓存恢复
```powershell
python -m work2.scripts.cache_degradation_features --clean-manifest outputs/manifest_vctk.jsonl --output-dir outputs/estimator_cache --seed 2026
```

### 恢复逻辑
- 如果中途中断：重新跑同样命令
- 会自动读取 `index.jsonl`
- 跳过已经完成的样本

---

## 退化估计器训练恢复
```powershell
python -m work2.train.train_degradation_estimator --cache-dir outputs/estimator_cache --epochs 50 --seed 2026 --resume
```

### 恢复逻辑
- 从 `last_model.pt` 恢复
- 自动读取已训练 epoch
- 继续训练到目标 epoch

---

## adaptive residual 训练恢复
```powershell
python -m work2.train.train_adaptive --epochs 50 --seed 2026 --resume
```

### 恢复逻辑
- 从 `last_model.pt` 恢复
- 自动继续训练

---

# 13. 目录重点说明

## 关键目录
- `work2/`：所有核心代码
- `outputs/estimator_cache/`：正式训练缓存
- `outputs/degradation_estimator/`：退化估计器训练输出
- `outputs/adaptive_residual/`：残差训练输出
- `outputs/pipeline_verify/`：真实数据小规模验证
- `data/vctk_16k/`：正式 16kHz 真实语音数据

## 文档入口
- `HANDOVER.md`：原交接文档
- `deep-research-report.md`：研究定位与创新评估报告
- `项目总路线与进展总览.md`（本文件）

---

# 14. 你现在最应该做什么

## 当前优先级（严格按照顺序）
1. **把缓存跑完**
2. **正式训练退化估计器**
3. **做特征来源实验**
4. **升级 residual 为轻量多频带专家**
5. **正式训练 residual**
6. **做核心对比 + 分组分析 + 消融**

---

# 15. 当前状态一句话总结

> **项目工程框架已经搭建完整，真实 VCTK 全量数据也已准备完成；当前正处于“正式缓存即将完成，接下来进入退化估计器正式训练”的关键阶段。**

---

# 16. 如果你明天/下次打开项目

直接做下面这 4 步：

```powershell
cd D:\workshop\G1
$env:KMP_DUPLICATE_LIB_OK = "TRUE"

# 1. 检查缓存是否完成
python -c "import json; lines=[json.loads(l) for l in open('outputs/estimator_cache/index.jsonl') if l.strip()]; print(len(lines))"

# 2. 如果没完成，继续缓存
python -m work2.scripts.cache_degradation_features --clean-manifest outputs/manifest_vctk.jsonl --output-dir outputs/estimator_cache --seed 2026

# 3. 缓存完成后，训练 estimator
python -m work2.train.train_degradation_estimator --cache-dir outputs/estimator_cache --epochs 50 --seed 2026

# 4. 之后再训练 adaptive residual
python -m work2.train.train_adaptive --epochs 50 --seed 2026
```

---

**文档状态**：已整理完成，可作为项目总导航文档长期使用。
